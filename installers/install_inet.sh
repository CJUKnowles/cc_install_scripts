# 1. Ensure that omnetpp can be source and ~/omnetpp/samples exists

# 2. clone the inet4.5 repo into ~/omnetpp/samples/ 
#  git clone git@github.com:inet-framework/inet.git

# 3. follow the inet4.5 instructions for install (python/install.md)

# 4. Verify it worked correctly (probably just by sourcing) then finish.